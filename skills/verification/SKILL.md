# VerificationSkill

Run the relevant test command in the isolated workspace. Report command, exit code, stdout, stderr, and whether every changed file is in the allowed set. A failed verification returns the patch to Coding rather than declaring completion.
